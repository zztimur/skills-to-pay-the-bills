interface:
  display_name: "Statements to Interest"
  short_description: "Extract statement interest for tax support"
  default_prompt: "Use $statements-to-interest to extract interest from one institution's text PDF statements for one tax year, generate the PDF support packet, and pause for FX confirmation when needed."
