interface:
  display_name: "Statements to Interest"
  short_description: "Extract statement interest for tax support"
  default_prompt: "Use $statements-to-interest to analyze one institution's machine-readable statement PDFs for one tax year, review extracted interest rows, apply FX only when needed, and generate an IRS-oriented support packet."
