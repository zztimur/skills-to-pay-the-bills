interface:
  display_name: "Get Year-End FX Rate"
  short_description: "Find a cited FBAR year-end FX rate"
  default_prompt: "Use $get-year-end-fx-rate to find a Treasury/Fiscal Data year-end USD exchange rate for one completed calendar year; classify network failures, preserve the exact fetch plan, and use raw JSON offline replay when needed."
