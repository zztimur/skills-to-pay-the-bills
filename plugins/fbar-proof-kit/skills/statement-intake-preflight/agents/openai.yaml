interface:
  display_name: "Statement Intake Preflight"
  short_description: "Preflight statements and certificate evidence"
  default_prompt: "Use $statement-intake-preflight to preflight these statement or certificate PDFs for one tax year, preserve date roles and source bindings, and identify every downstream review gate."
