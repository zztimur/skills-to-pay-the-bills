interface:
  display_name: "Privacy Gate"
  short_description: "Block secrets and private data before commits."
  default_prompt: "Use $privacy-gate to scan staged or local files for secrets and private data before committing."
