interface:
  display_name: "Get Year-End FX Rate"
  short_description: "Find a cited FBAR year-end FX rate"
  default_prompt: "Use $get-year-end-fx-rate to find a Treasury/Fiscal Data year-end USD exchange rate for one currency and calendar year; if the API is unavailable, retain its raw JSON response and rerun with --api-file."
